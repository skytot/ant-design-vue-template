export const getUserInfo = state => {
    return state.userInfo
}
export const getUserStatus = state => {
    return state.userStatus
}
export const getUserToken = state => {
    return state.userYoken
}
